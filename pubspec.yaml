name: product_cart_app
description: "An app with purpose of view products and able to add to cart"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  get: ^4.7.3
  dio: ^5.9.0
  google_fonts: ^6.3.2
  shared_preferences: ^2.4.0
  intl: ^0.19.0
  flutter_slidable: ^3.1.0
  flutter_screenutil: ^5.9.3
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  pdfx: ^2.9.2
  logger: ^2.6.2
  http: ^1.6.0
  flutter_rating_bar: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true